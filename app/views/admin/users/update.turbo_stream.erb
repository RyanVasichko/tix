<%= turbo_stream.replace "application_modal_body" do %>
  <%= render "admin/users/form", user: @user, url: @user.is_a?(User::Customer) ? admin_customer_path(@user) : admin_admin_path(@user) %>
<% end %>
