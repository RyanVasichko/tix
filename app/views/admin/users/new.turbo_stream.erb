<%= turbo_stream.replace "application_modal" do %>
  <%= render "shared/modal", max_w: "max-w-xl", title: "Add #{@user.is_a?(Users::Customer) ? 'Customer' : 'Admin'}" do %>
    <%= render "admin/users/form", user: @user, url: @user.is_a?(Users::Customer) ? admin_customers_path : admin_admins_path %>
  <% end %>
<% end %>
