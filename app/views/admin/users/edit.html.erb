<%= turbo_frame_tag "application_modal" do %>
  <%= render "shared/modal", max_w: "max-w-xl", title: "Edit #{@user.name}" do %>
    <%= render "admin/users/form", user: @user, url: @user.is_a?(Users::Customer) ? admin_customer_path(@user) : admin_admin_path(@user) %>
  <% end %>
<% end %>
