<div class="card max-w-7xl">
  <h1 class="text-center text-3xl font-semibold"><%= @show.artist.name %> - <%= short_date(@show.show_date) if @show.show_date %></h1>
  <%= render "form", show: @show %>
</div>
