<% form = DoseyDoeTicketsFormBuilder::FormBuilder.new(:show, Show::GeneralAdmissionShow.new, self, {}) %>

<%= turbo_stream.append "general_admission_show_sections" do %>
  <%= form.fields_for :sections, Show::GeneralAdmissionSection.new, child_index: random_index do |section_fields| %>
    <%= render "section_fields", section_fields: section_fields %>
  <% end %>
<% end %>